#include <bits/stdc++.h>
using namespace std;

int main(){
    int nums [] = {10, 20, 30, 40, 50};
    //Range based for loop - executes for loop over a range of values, such as all elements in an array
    for (int num: nums)
        /* code */
        cout << num<< " "; //each element in the array is printed
        return 0;
}