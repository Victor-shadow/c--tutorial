-In C++ data can be sent to functions when they are called in order to perform operations
which are called parameters or arguments
*Formal Parameters(only parameters) - Variables used in parameters list of a function as placeholders
*Actual Parameters - The expressions or values passed during a function call
