# Functors are objects that can be treated as though they are functions or
# function pointers
# A functor(function object) is a C++ class that acts like a function
# To create a functor, create an object that overloads the operator()

# Functor(value);
# Functor.operator()(value);

