#include <iostream>
#include <stack>
using namespace std;

int main(){
    stack<int> st;
    //Insert elements at the top of the stack
    st.push(100);
    st.push(200);
    st.push(300);
    st.push(400);
    return 0;
}